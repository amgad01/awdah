#!/bin/sh
set -eu

if [ "${CI:-}" = "true" ] || [ "${GITHUB_ACTIONS:-}" = "true" ]; then
  exit 0
fi

if [ "${RUN_FULL_PRE_PUSH:-0}" = "1" ]; then
  exec env RUN_E2E=1 ./scripts/ci/pre-push-checks.sh
fi

exec ./scripts/ci/pre-push-quick.sh
